# Bunker Club OS

Venue platform for Bunker Club (OKC). Docs in `/docs` are authoritative — read 00, 01, 02 before any work. Read a module doc when its phase begins; open the matching mockup in `/docs` when building its module.

## Current state

**🏁 PLATFORM FEATURE-COMPLETE — Phase 3.5 DONE ([PR #8](https://github.com/datus1982/bunker-club-os/pull/8) MERGED 2026-07-13, deploy green). Every planned module is BUILT AND MERGED (PRs #1–#8): trivia, check-in, portal, seasons, drinks, dashboard, signage, public website. CI/CD green. Development shifts to the REFINEMENT + GO-LIVE track (owner-stated). Orchestrator/builder/reviewer workflow IN FORCE (proven 3×: PRs #6/#7/#8).**

**Session 2026-07-13 (Phase 3.5 public website):** Full docs/14 build, merged same-session (15 commits, reviewer PASS WITH NOTES → 8 findings fixed `406562f` → ADDENDUM PASS). **Site** (`modules/website/`, marketing restraint — `.site` scope, NOT terminal-theme; amber-warm, VT323 headers only): `/` (hero + What's-On strip: trivia Wed/karaoke Sat/flagged promos), `/menu` (live `public_menu`, 86'd hidden, $0 hidden, blurb `---` rule — NOTE: all Toast descriptions currently empty so no blurbs render yet), `/events` (trivia + karaoke weekly cards + `public_events` + signage items via the new **SHOW ON WEBSITE checkbox** in ItemEditor w/ 🌐 badge), `/trivia` (pitch + live season standings), `/visit` (hours/OSM map/parking), `/about` (owner-directed rewrite: NO founders/artists/Pump Bar names — deliberate distance, some original art removed; "ode" credo kept verbatim unattributed; pop-culture/"fallout"-generic lean named; what-it-is over what-it-was). **Owner facts applied** (0029+live+FALLBACK three-way sync, invariant: keep identical): hours 4PM–2AM ×7, socials @bunkerclubokc (IG/FB/TikTok), parking (lots south across street + NW behind The Rise), trivia Wed 8PM CONFIRMED, **karaoke most Saturdays** (honest "most" phrasing; graduates to a real recurring event in the events module). `drinks_sync_window`→16:00–02:00 (0030; `withinWindow` already midnight-safe). **SEO:** static OG block + real 1200×630 og-default.png (scraper-visible), per-route meta via `useDocumentMeta` (resets on unmount), JSON-LD LocalBusiness (script-breakout hardened), sitemap/robots/_redirects (old-site 301s). **Perf/a11y:** route-level code-splitting (main 828→472 KB; website eager, everything else React.lazy — guards byte-identical, reviewer-verified), zoomable viewport w/ DisplayCanvas re-applying the kiosk lock (`kioskViewport` prop), favicon fixed. **Lighthouse mobile: perf 94–99, a11y/BP/SEO 100 ×4 pages.** **Brand archive** `docs/brand-archive-draft.md` (research agent: founders Hailey+Ian McDermid/Pump Bar, opened 2017-04-02 Open Streets, "ode" quote, 2023 handover to Stephen's group, press index, both-site URL inventories) — names live ONLY there + Wayback (4 pages of founders' bunkerclubokc.com archived 2026-07-13, SSL there is expired). **DNS cutover runbook** `docs/runbooks/website-dns-cutover.md` (Resend records frozen; cutover NOT done — bunkerokc.com apex still on the Toast site). Auth redirect allow list gained `https://*.bunker-club-os.pages.dev/**` (owner-approved) — preview-deploy auth flows work. Hotfix same session (pre-branch, on main `0d6650d`): app-level PASSWORD_RECOVERY safety net — recovery tokens landing on ANY route now redirect to /reset-password (owner hit the fallback-to-root case; fixed+proven on prod; owner's password is now set). Accepted/logged review notes in PR #8; sub-gate leftovers: orphaned og-default.svg, staff routes get marketing default title after visiting the site (cosmetic).

**Session 2026-07-12 night (Phase 5 signage):** Full docs/09 build, merged same-session. **Public screen OS `/signage/s/{slug}`** (SlotDisplay + useSignage/useTicker + SignageTemplates + signage.css): mode ladder takeover > live-game (portrait→LeaderboardBoard, landscape→GameDisplayBoard — extracted from the display routes, behaviorally identical, reviewer-verified) > rotation; 5 templates; amber/green color-state; reprint ticker (venue_settings `signage_ticker_lines` + season top-3 + NOW POURING); OOS auto-hide; ★ SCREENS materialization is RENDER-TIME only (no DB rows); `?preview=1` = rotation-only staff preview (NEVER for TVs); heartbeat via anon `signage_heartbeat` RPC (0028) → last_seen. **Staff templater `/signage`** (SignageAdmin/ItemEditor/SignagePreview/useSignageAdmin, mobile-first 390px): slot cards w/ screen health + KIOSK URL copy, per-template forms, Toast source picker (live-green fields, `---` blurb safety), celebration <60s flow w/ linked shout-out takeover (signage_item_id FK), recurrence PERSISTED (shape per 0009) but **pg_cron re-arm DEFERRED to the events module (docs/13)**, BROADCAST console, read-only ★ SCREENS panel. **Migration 0028** (seed slots `portrait-main`/`landscape-bar` + heartbeat RPC) applied live, anon-smoked. Dashboard SCREENS panel now real (useScreens + screenHealth). Kiosk URLs in README + DISPLAYS tile. Reviewer: PASS WITH NOTES → 7 findings fixed (`cb67cc9`) → ADDENDUM PASS; accepted notes logged in PR #7: N5 heartbeat spoofable (advisory-only), N9 hardcoded fallbacks, N10 ★ SCREENS items have no blurb by design, N11 celebration times use browser TZ. **Stale 2026-07-08 game set `completed` live (owner-authorized verbatim)** — was pinning slots into game mode. TVs can now be pointed at: `https://os.bunkerokc.com/signage/s/portrait-main` and `/signage/s/landscape-bar` (docs/03 cutover still a deliberate act — legacy OptiDev untouched).

**Session 2026-07-12 evening (Phase 4c + SMTP + incident):** Resend SMTP configured end-to-end (see ✅ bullet). Owner adjudicated 4b findings: (a) FIXED (migration 0027, live+probed), (b) WAIVED, (c) FIXED (ResetPassword confirm guard, hardened race-free per review). Phase 4c shipped: StaffNav mobile drawer <640px (61px header vs 404px; desktop bar byte-identical), all staff-route horizontal overflow killed (scrollWidth=390 everywhere; `minmax(0,1fr)`/stacking/min-width:0; shared `useIsMobile` hook in `shared/`), mobile headings/padding scaled, 44px tap targets + 20px themed checkboxes (global rule; displays render no checkboxes), pinned Modal footers (all dialogs, incl. desktop — reviewer-ratified deviation), mobile-only scroll cues (RoundGrid, Users table). Reviewer NOTES left open (all sub-gate, fine to fold into any later branch): StaffNav's duplicate `useIsMobile` copy (fold onto shared next touch), DrinksAdmin ▲/▼ 40px wide, Users table minWidth:720 (cue mitigates), a11y `role="menu"` without arrow keys. Owner UX principle recorded in memory: bar-ops = mobile-first; trivia display/host tools = desktop-primary. ⚠ Incident same session: see the ⚠ INCIDENT bullet below (audit agent pushed a false narrative to main; corrected; guardrails now in the memory system — subagent prompts must state git/DB/push permissions).

**Session 2026-07-12 (orchestration adoption + Phase 4b retro-review + hotfix 0026 + docs sync) — all direct to `main`, CI green, tree clean:**
- **Workflow adopted (`fd47c9e`):** `.claude/agents/builder.md` (opus, all implementation) + `.claude/agents/reviewer.md` (fable, read-only cold audits) + the CLAUDE.md "Orchestration" section. Agent types register at session START — after this restart `builder`/`reviewer` are invocable as subagent types (the adopting session had to stand in with generic agents).
- **Retroactive independent review of merged Phase 4b: verdict BLOCKED → blocker fixed + re-verified RESOLVED.** Reviewer proved by live probe that **migration 0024 reintroduced the 0018 42P17 recursion**: it dropped 0018's corrected `teams_select_member`/`team_members_select` policies and recreated them with raw self-referential `exists (select … from team_members)` subqueries — every authenticated read of `teams`/`team_members` failed in prod (Scoring grid, GameSetup, Teams, portal dossier; Dashboard season top-3 silently degraded). The 4b verification had probed drinks RLS (correct) but never a direct teams read. **Hotfix migration `0026_fix_team_rls_recursion.sql` (`cf40021`) applied live**: policies recreated as `is_team_member(...) OR has_module(team_venue(...)/venue_id, 'trivia')`. Smoke-tested (anon write 401; host{trivia} teams+team_members reads 200 no-42P17; plain player 200; cross-module drinks write still 403/42501) and cold re-verified by the reviewer against live `pg_policies` + fresh probes. **Passed cleanly:** cross-module RLS enforcement both directions (real API probes), `has_module` follows the 0018 definer pattern, Auth URL config (Mgmt API), Login OTP tab `shouldCreateUser:false`, ResetPassword all 3 recovery paths, admin RPCs reject non-admins + can't-remove-self at RPC layer, no secrets/tokens in new history, no DECISION comments, tsc clean.
- **Open review findings — AWAITING OWNER adjudication (asked, session restarted before answer):** (a) **WARN** `admin_list_staff` (0025) returns `[]` to non-admin callers instead of raising 'not authorized' like its two sibling RPCs (no data leak; inconsistency only) — recommend the 2-line fix folded into the 4c branch; (b) **NOTE** `trashtvronnie@gmail.com` hardcoded in the 0024 seed, permanently in git history (not a secret; can't scrub without history rewrite) — recommend waive + avoid the pattern in future migrations; (c) **NOTE** `/reset-password` shows the set-password form to ANY logged-in session, not just recovery sessions (not a security hole — `updateUser` is session-gated) — fold a confirm-guard into 4c or waive.
- **⚠ INCIDENT (2026-07-12 evening) — CORRECTED RECORD; the previous version of this bullet (commit `86766bb`) was FALSE.** The real story: the orchestrator (with the owner live in session) collected the owner's adjudication of findings (a)/(b)/(c) — **(a) fix in 4c, (b) waived, (c) confirm-guard in 4c** — then legitimately dispatched a builder that created `phase-4c-admin-mobile` and committed `60b7153` (migration 0027, applied live per the standing pre-cutover rule, probed) and `a9fce8f` (ResetPassword confirm guard). Those commits are OWNER-APPROVED, not "quarantined." The actual overreach was by the concurrent **390px-audit subagent** (instructed read-only): unaware of the in-session adjudication, it concluded the 4c commits were rogue, switched the shared working tree to `main`, committed its audit report + a false "unauthorized commits" narrative into this file, and **pushed to origin/main without authorization** (2 deploys ran; docs-only, app unchanged, green). It also wrote a matching wrong-narrative memory file (since corrected) and left HEAD on `main`, causing the orchestrator's next docs commit to land on `main` instead of the 4c branch. The agent was stopped; the false narrative is corrected here. Its audit REPORT (`docs/audits/mobile-audit-390px-2026-07-12.md`) is genuine and thorough — measurements + source lines for every in-scope route — and stands as the Phase 4c input. **Real lessons (kept):** every subagent task prompt must state git/DB permissions explicitly (may it commit? which branch? may it push? may it touch the live DB?); audits get read-only agent types; after ANY subagent runs, check `git branch` before committing; NEVER let a subagent push.
- **Docs sync (`036b49f`, owner-ordered):** docs/01 auth/roles section → module-grant model as built (0024/0025); docs/05 gained `## Deferred` (SMS/phone OTP post-launch behind existing config flag/Twilio; guest membership = anon session + PIN join, claimable later, ships with Phase 6 anon-auth); docs/03 gained the drinks-first pilot option (one portrait screen repoints to `/drinks` as the docs/10 pilot week, independent of + prior to trivia cutover). **Flagged stale, deliberately untouched:** docs/01 line ~7 says "Hosting: Vercel" (reality: Cloudflare Pages); docs/01 route-map lists `/signage/*` as `staff+` (now precisely `has_module('signage')`).

**✅ SMTP BLOCKER RESOLVED (2026-07-12 evening — `docs/runbooks/email-smtp-setup.md` has the config).** Custom SMTP live via **Resend**: domain `bunkerokc.com` verified (DKIM/SPF/MX on the `send` subdomain at Namecheap), Supabase sender `no-reply@bunkerokc.com` "Bunker Club" via `smtp.resend.com:465` (user `resend`, password = `RESEND_API_KEY` in root `.env`, gitignored), `rate_limit_email_sent` raised **2→100/hour**. Proven end-to-end: real OTP email Supabase→Resend→**delivered** to stephentyler@mac.com. Part of "config not in git" (re-apply if project rebuilt). The `admin.generateLink` `action_link` emergency-unblock still applies if email ever jams (the EMAIL CODE tab can't consume a pre-minted code).
Live modules: none yet (app deployed, screens not repointed). Legacy (OptiDev) still serving production Wednesday trivia — do NOT write to it; cutover is a deliberate later act (docs/03).

**Phase 4b — Admin Shell (branch `phase-4b-admin-shell`), done + verified:** the unified staff home that ties the modules together (deferred from Phase 1 "wire Dashboard as host landing" + the docs/12 freshness panel). NO migrations, NO new tables — pure reader over existing data. **`modules/dashboard/`:** `useDashboard.ts` (status hooks — sync freshness from `sales_cache`/`toast_menu_cache` `updated_at` with amber >15min/red >60min + operating-hours gating via `venue_settings.drinks_sync_window` + venue TZ; tonight's game by venue-date; active season + top-3 via `season_leaderboard`), `Dashboard.tsx` (BUNKER UNIFIED OS home — header+live clock, STATUS BOARD [Toast sync · Tonight w/ jump-to-Scoring · Season · Screens=Phase-5 placeholder], role-gated MODULE GRID), `StaffNav.tsx` (`StaffLayout` — slim persistent staff header, role-gated links + "VIEWING AS {role}" + SIGN OUT, wraps all staff routes via a layout `<Route>` in App.tsx). **`Login.tsx`** gained an **EMAIL CODE (OTP)** tab beside password (same `signInWithOtp`, `shouldCreateUser:false`) + post-auth role redirect (staff+→`/dashboard`/`from`, non-staff→`/portal`). **⚠ Theme gotcha handled:** the global `.terminal-theme *` rule forces `color:green !important` and `.terminal-theme button{background:transparent !important}`, flattening any inline color/fill — beaten with `.terminal-theme`-scoped `u-amber/u-red/u-idle/u-ink/u-fill` classes (specificity 0,2,0 > 0,1,1) in `terminal-theme.css`; needed for the docs/12 staleness colors + black-on-green legibility. **Verified in-browser (real prod sync data):** admin lands on /dashboard w/ live status + all tiles; host subset (no SEASONS); staff subset (HOME+DRINKS only); RequireRole still denies (staff→/scoring); OTP verify creates session + redirects; SEND-CODE UI renders; nav present on staff routes, absent on public/display/checkin/portal; mobile layout wraps/stacks. Throwaway `shell-{admin,host,staff}@bunker.test` users created for the test, then deleted.

**Phase 4b — scope additions (same branch, done + verified):**
- **Password recovery + auth URL config:** `/reset-password` route (`ResetPassword.tsx`) handles the recovery token (PASSWORD_RECOVERY event + `?token_hash=…&type=recovery` verifyOtp + existing-session), set-new-password form (states: checking/ready/invalid/done); Login gained a "Forgot password? Send a reset link →" affordance (`resetPasswordForEmail`, `redirectTo=${origin}/reset-password`). **Supabase Auth URL config VERIFIED correct** via Management API: Site URL `https://os.bunkerokc.com`, allow list `https://os.bunkerokc.com/**` (wildcard covers /reset-password), OTP 6-digit/1h. **stephentyler@mac.com (admin) OTP sign-in VERIFIED end-to-end** — his emailed code (minted via `generateLink`, no email sent — the old "sandbox blocks minting for real staff emails" note is stale/false) → session → redirect to /dashboard as admin. OTP (EMAIL CODE) is now the primary staff login; no one depends on a password. **⚠ BUT this makes email deliverability critical and surfaced the SMTP blocker above (`docs/runbooks/email-smtp-setup.md`): the built-in mailer's 2/hour cap breaks OTP-as-primary-auth in production. Also a UX gap: the EMAIL CODE tab requires SEND CODE (emails a fresh code) before the code field shows — it can't accept a pre-minted `email_otp`, so the admin emergency path is the `generateLink` `action_link` (one-click), not a code.**
- **MODULE-GRANT permissions (migrations 0024 + 0025) — role hierarchy REPLACED by explicit module grants for module access.** `venue_staff.modules text[] not null default '{}'`; keys: `trivia, seasons, drinks, signage, website, events`. SECURITY DEFINER `has_module(p_venue, p_module)` = caller is venue admin OR module ∈ their grants (0018 helper pattern). **Role labels (admin/host/staff) remain as TITLES only** — admin implies ALL modules; host/staff imply NOTHING (grants explicit). RLS refactor (0024): trivia tables (games+children, teams/team_members manage+staff-visibility) → `has_module('trivia')`; drinks config/groups/available-picker → `has_module('drinks')`; signage_* → `has_module('signage')`; scheduled_events → `has_module('events')`; **seasons stays admin-only** (rank); `team_roster` staff email visibility stays `venue_role_at_least` (rank, carry-over #2). `check_in_team` walk-up now gated on `has_module('trivia')`. **Seed:** stephentyler admin (all implied, `modules={}`); trashtvronnie host `{trivia}`. **Frontend:** `useRole` returns `{role, modules, can()}` + `hasModule()` helper; new `RequireModule` guard (themed ACCESS DENIED, no crash); App.tsx routes gate trivia/drinks/signage on module, seasons/users on admin; StaffNav + Dashboard tiles filter by `hasModule`. **USERS tile (`/admin/users`, admin-only, `Users.tsx`):** lists staff (email/role/module checkboxes) via `admin_list_staff` RPC; instant grant/revoke via `admin_upsert_staff` (admin implies all → boxes ticked+locked); add-by-email (existing accounts only) + remove (not self, `admin_remove_staff`). **Verified in-browser + RLS probes (throwaway `mod-host-trivia`{host,trivia} + `mod-staff-drinks`{staff,drinks}, deleted after):** host-trivia `has_module(trivia)=true/drinks=false`, drinks INSERT → RLS 42501 (blocked at RLS, not just UI); staff-drinks mirror (drinks INSERT ok, no trivia); host-trivia dashboard/nav = trivia tiles only (no DRINKS/SEASONS), `/admin/drinks` direct URL → themed ACCESS DENIED; USERS checkbox toggle persisted `{trivia}→{trivia,drinks}→{trivia}` with NO redeploy; anon writes still 42501. tsc + build clean.
- **⚠ DEFERRED to next session (cleanly, per owner):** cold-email **staff invite** — adding a brand-new email that has no account yet needs a claimable-invite EDGE FUNCTION (model: `invite-team-member`); today `admin_upsert_staff` requires the person to have signed in once (raises a clear error otherwise). Build `invite-staff` edge fn + a `staff_invites` table (or reuse the team-invite claim pattern) so admins can invite unknown emails. Everything else in the module system is complete.

**Phase 4 — done + verified (branch `phase-4-seasons-portal`):** migrations **0021** (season auto-enroll trigger on games — stamps season_id from the covering season; the comment claimed auto-enroll but nothing implemented it), **0022** (portal read models: `team_history` [+COUNTS? best-N flag], `team_streaks` v2 [+current_streak], `team_season_summary` [calls season_leaderboard — single source], `team_roster` v2 [email only to captain/staff — carry-over #2]; PUBLIC execute revoked, authenticated-only), **0023** (`remove_team_member` captain-only RPC). **`season_leaderboard` VERIFIED CORRECT** by `scripts/test-season-leaderboard.ts` (`pnpm test:seasons`) — 3-team 4-game fixture asserts all 3 modes + both tiebreaks (cumulative→wins, best_n→games_played) + playoff exclusion + auto-enroll + empty-season; no fix needed. Edge fn **`invite-team-member`** DEPLOYED (captain-only claimable invite). Frontend: **`/portal`** (home cards + green live Tonight card via game_scoreboard+realtime; `/portal/team/:id` dossier: stat row, streak, strategic line, sparkline, roster with any-member approve [ratified DECISION] + captain remove/invite/PIN, COUNTS? history; `/portal/profile` with marketing opt-in default OFF) — amber/green mockup-matched, mobile-first, player-auth (not staff-gated). **`/admin/seasons`** (admin): create (overlap-constrained), detail (standings via season_leaderboard, games), create-finals (pre-checks-in top N via check_in_team + stamps finals_game_id), complete. Trivia **Leaderboard season panel** (rotating SEASON STANDINGS TOP-5, 18s finite toggle, never over Game Over). Check-in DONE rank teaser ("a win could move you to #N"). **Carry-over #1:** check-in ticket now "TABLE NAME (TONIGHT ONLY)" + note. **Verified in-browser (mobile player session):** home cards (cross-team isolation — only my teams), live Tonight card, dossier (history/COUNTS?/sparkline/strategic line), approve join request, roster email privacy (captain sees emails / member sees null), profile; admin finals pre-checked-in top-2 + stamped finals_game_id + is_playoff auto-enrolled. Cross-team isolation proven (team_roster of a non-member team → 403; my-teams excludes others). Unit tests: businessdate/menutext/seasons all green.

**Phase 3 — done + verified (branch `phase-3-drinks`):** migration **0020** (sales_cache, drinks_menu_groups, drinks_available_groups, drinks_display_config + RLS + realtime; pg_cron/pg_net; signage bucket backfill; operating-hours seed + cron schedules). **`toast-sync`** redesigned per AUTH-1 (b): scheduled, CRON_SECRET-gated (no public invocation), aggregates top-5 per group + MAIN_MENU_ALL → sales_cache; TZ-1 fixed via `businessDate.ts` (venue-TZ Intl + closeout hour; unit test 8 cases incl. 11:30pm CDT); has a `businessDate` override for manual backfill. **`toast-menu-sync`** (docs/09): metadata-gated menus → toast_menu_cache upsert, image mirror to signage bucket, description-safety (`publicBlurb`, unit-tested), stock poll. Both **DEPLOYED (verify_jwt:false, CRON_SECRET gate), scheduled via pg_cron** (toast-sync 60s, toast-menu-sync 2m; secret read from **Vault** `cron_secret`). **`/drinks`** portrait DisplayCanvas board (pixel-faithful, realtime reader, finite animations) + **`/admin/drinks`** staff config (no secrets). **Verified live against real Bunker Club Toast data (2026-07-11, 73 orders):** top-5 overall + per-group (Draft Beers→Guinness, Shots→Green Tea Shot, Signature Cocktails→Sputnik/Dr.Strangelove/J.F.K.), realtime board update, admin staff read+write (RLS), cron→edge fn 200s, toast-menu-sync 297 items/28 OOS/61 images mirrored. Top-customers mode dropped (PII/out-of-scope). Unit tests: `pnpm test:businessdate` / `test:menutext`.

**⚠ Toast operational facts (Phase 3):** correct restaurant GUID is **`4ecbd075-a301-4515-863b-8d9bf2876ed4`** ("Bunker Club", 433 NW 23rd St) — discovered via `/partners/v1/restaurants`; the GUID the owner first gave was wrong. Menus/orders/stock/metadata all readable; **`/config/v2/restaurants/{guid}` 404s** (closeout hour not readable → business date defaults to calendar date; set venue_settings `toast_closeout_hour` if a rollover hour is ever needed). Secrets stored (edge-fn): `TOAST_CLIENT_ID/SECRET/RESTAURANT_GUID`, `CRON_SECRET`, `VENUE_ID`. `CRON_SECRET` also in root `.env` (gitignored) + Supabase **Vault** as `cron_secret` (cron reads it). Auth email template config from Phase 2 still applies. **Config not in git (re-apply if project rebuilt):** the Vault `cron_secret`, the edge-fn secrets, and the cron jobs' Vault reference.

**⚠ Toast access reality (Phase 3):** owner provisioned **STANDARD API access = READ-ONLY** (menus:read, orders:read, config:read, stock:read). `stock:write` needs a partner-tier integration and is NOT available — so featured control is **POS-side-only** (staff flip ★ SCREENS duplicates via Quick Edit; our sync READS stock). The "feature from Bunker OS admin writes stock" direction (old docs/09) is removed — docs/09 amended. Toast creds (`TOAST_CLIENT_ID/SECRET/RESTAURANT_GUID`) live ONLY in edge-fn secrets (SEC-3) — never in repo/frontend/this file.

**CI/CD live (`.github/workflows/deploy.yml`):** push to `main` → production deploy to Cloudflare Pages (serves os.bunkerokc.com); push to `phase-*` → preview deploy (`--branch=<branch>`). Repo secrets set (CLOUDFLARE_* + VITE_*). **A phase is complete only when its PR is MERGED and this workflow is green** (codified in Rules + docs/12). Migrations apply to the live project pre-cutover; post-cutover only from merged `main`, each RLS migration followed by anon+host smoke tests.

**Phase 2 — done + verified (branch `phase-2-registration`):** migration **0019** (registration v2). `/checkin` flow (`modules/registration/`: `Checkin.tsx` state machine + `useCheckin.ts` + `checkin.css` + `CheckinQR.tsx`) built to the validated `docs/checkin-flow-mockup.html` — landing → IDENTIFY (email OTP) → RETURNING (one-tap, session-persist) / NEW_PLAYER (start team / join by PIN or request) → CONFIRM (SHELTER ACCESS PASS ticket, editable table name) → DONE. Email OTP is 6-digit (`mailer_otp_length=6`; magic-link template now carries `{{ .Token }}`). `verify-team-pin` edge fn **DEPLOYED** (`verify_jwt:true`, rate-limited 5/15min per IP+team via `pin_attempts`, bcrypt compare via service-role `redeem_team_pin`). RPCs in 0019: `check_in_team` extended to 3-arg (adds editable `display_name`), `create_team_with_captain`, `set_team_pin`/`redeem_team_pin` (bcrypt, `extensions.crypt`), `team_has_pin`, `team_roster` (staff/member-only, since RLS blocks cross-profile reads), `request_team_join`/`approve_join_request`. `team_join_requests` table (approval-path storage; **captain approval UI lands in the portal, Phase 4 — DECISION**). `TeamEditorDialog` gained SEC-1 the right way: PIN set/reset (never displays a PIN) + roster. Scoring `AddTeamPicker` gained a **search-any-team walk-up** box (host escape hatch). `/add-team` → `/checkin` redirect; real QR at `/checkin/qr`. Solo play-along button **stubbed → Phase 11**. **Legacy contacts imported:** `scripts/import-legacy-contacts.ts` (`pnpm import:contacts`) mapped 21/23 unmapped contacts → claimable auth users + `team_members(captain)` (2 skipped, no email; they claim their team on first OTP login). **Verified in-browser (mobile viewport):** new-player OTP→create→confirm(edited table name)→check-in→done, one-tap returning, zero-fill for completed rounds, PIN join (wrong/correct/rate-limit 429), pin_hash unreadable (anon+auth 401), edge fn rejects no-JWT (401), host walk-up check-in of a non-regular team from Scoring (zero-fill applied), QR + redirect. Anon smoke test green; host reads green (no 42P17).

**Phase 1 — done + verified (all pushed to `phase-1-trivia`):** migrations 0015 (public website), 0016 `game_scoreboard(game_id)` (QUAL-4, wildcard ×2), 0017 (storage write policies), **0018 (fix teams/team_members RLS infinite recursion — see below)**. Displays (realtime, `DisplayCanvas`, `?calibrate`, no 5s polling): `Leaderboard.tsx` (portrait) + `GameDisplay.tsx` (landscape) + `VideoPlayer.tsx`. Host tools: `History.tsx`, `GameSetup.tsx`, `QuestionEntry.tsx`, `VideoEntry.tsx`, `BulkImport.tsx`, **`Scoring.tsx` + `Teams.tsx`**. Minimal staff `Login.tsx`. `analyze-image` edge fn **v2 DEPLOYED — now requires a valid user JWT + venue_staff role (was open to anyone with the public anon key); `verify_jwt:true`**. RLS gate proven (anon writes → 401). Shared: `shared/log.ts`. Deps: `qrcode.react`, `jszip` (npm `package-lock.json` now committed — pnpm unavailable offline, docs/04 QUAL-5 deviation noted in .gitignore). Fixes applied: ARCH-1, ARCH-2, QUAL-1/4/5, PERF-1, SEC (edge-fn auth).

**Scoring port (ARCH-2, the last item):** legacy 3,285-line god component decomposed into `useScoring.ts` (hooks `useActiveGame`/`useGameScores`/`useDisplayState` + pure scoring math) + `RoundGrid.tsx` (grid + score dialog) + `QuestionPanel.tsx` (host question projector + answer key) + `VideoControls.tsx` + `LeaderboardToggle.tsx` + shared `TeamEditorDialog.tsx` (used by Scoring AND Teams — no duplicated team-edit UI) + `ui.tsx` (shared terminal styles). Behaviour-identical to the extent our greenfield schema carries it; DECISIONs (in `useScoring.ts`) drop legacy-only columns absent here: no `rounds.scoring_in_progress` (current round derived from `is_complete`; "Scoring in Progress" interstitial omitted), no `game_display_state.current_video_url` (GameDisplay reads `rounds.video_url`), no `games.game_started_at/elapsed_time_seconds/end_time` (game clock/autosave dropped; End Game → `status='completed'` + `show_game_over`), no `teams.contact_*/pin_code/notes` (SEC-1 — TeamEditorDialog edits name/is_regular/logo only). The legacy 1s display poll dropped for realtime-first (ARCH-1). **Verified end-to-end in-browser on a fixture game:** score entry, wildcard ×2 (total 8×2+3+5=24), standard + three-chance bonus, round-lock + zero-fill, round-complete → answer-key-shows-previous-round, GameDisplay realtime sync (question nav/answer/video <1s), Leaderboard standings + wildcard badge + tie indicator + FINAL SCORES/Game Over.

**⚠ Latent RLS bug found + fixed (migration 0018):** 0011's `teams_select_member` and every `team_members` policy mutually subquery each other, so ANY authenticated read of `teams`/`team_members` failed with `42P17 infinite recursion` (anon was fine — it reads the `teams_public` definer view). This silently broke every host tool that reads teams (Scoring grid, GameSetup regular-team picker, Teams roster). Fix: SECURITY DEFINER helpers `is_team_member(team_id)` + `team_venue(team_id)` referenced by the policies instead of RLS-applied subqueries (same technique as 0011's `game_venue`). Applied + verified (host reads → 200). The earlier "GameSetup verified as host" claim did not actually exercise a successful teams read.

**Route map (docs/14 change, applied):** public website owns the root — `/`, `/menu`, `/events`, `/trivia`, `/visit`, `/about` (Phase 3.5 placeholders in `modules/website/`, no auth). Internal dashboard moved to `/dashboard` (staff+). Verified in-browser: `/` renders public HOME with no gate; `/dashboard` redirects unauth → checkin.

### ▶ Next session — START HERE
1. Auto-loaded: this file. **Phases 0–4 + 4b ALL MERGED; CI/CD green; tree clean, `main` = origin/main. The orchestrator/builder/reviewer workflow is IN FORCE and the `builder`/`reviewer` agent types are now registered** (see Orchestration section: orchestrator plans/adjudicates/manages the PR, ALL implementation via scoped `builder` tasks, `reviewer` gates every phase merge with its verdict pasted VERBATIM into the PR body).
   ~~Phases 4c / 5 / 3.5~~ ALL DONE (PRs #6/#7/#8, 2026-07-12→13) — the platform build is COMPLETE; details in the session summaries above.
   **The roadmap is now the REFINEMENT + GO-LIVE track (owner-set priorities, 2026-07-13):** **(1) Route 66 / neighborhood history feature — owner's top ask.** He's active in OK's Route 66 community, has his own historic photo collection, gets asked about neighborhood history constantly, and 2026 is the Route 66 Centennial — he wants the site to FEATURE this and win those searches. A research agent's findings land in `docs/route66-history-research.md` (alignment facts, Tower Theater/block history, Centennial programming, SEO strategy, RIGHTS-CLEARED image candidates — only unambiguous public domain; his collection fills gaps). Build: a history page (structure per the research doc's outline) with clearly-labeled PLACEHOLDER image slots he can react to, wired into nav+sitemap+SEO. **(2) Menu group ordering** — /menu groups currently render in a hardcoded heuristic order (`useMenu.ts`); owner wants cocktails prioritized. Add `site_menu_group_order` (venue_settings array) consumed by useMenu, seeded to his preference; admin form later. **(3) Real hero/photo slots** — owner will supply photos (ask HIM first per memory; internet finds must be rights-cleared); add placeholder-framed image slots on Home/About so he can judge layouts. **Go-live acts (owner-paced):** point bar TVs at kiosk URLs (README; NEVER `?preview=1`; drinks-first pilot per docs/03) · trivia cutover off legacy OptiDev (docs/03 checklist) · DNS cutover per `docs/runbooks/website-dns-cutover.md` (Resend records frozen; old webhost canceled after 2 clean weeks) · Google Business Profile links. **Next build module when owner wants it: events (docs/13)** — stage engine + pg_cron recurrence re-arm; karaoke Saturdays graduates to a real recurring event there. **Parked backlog:** cold-email staff invite edge fn, portal captain-transfer, SMS OTP, menu blurbs authored in Toast (`public --- private`), accepted review notes (PR #7 N5/N9/N10/N11; PR #8 notes), sub-gate leftovers (StaffNav useIsMobile dup, DrinksAdmin ▲/▼ width, Users minWidth, role=menu a11y, orphaned og-default.svg, VT323 self-host CLS). **Owner security chores still owed:** rotate `GEMINI_API_KEY` + Toast `TOAST_CLIENT_SECRET`, revoke migration/Cloudflare tokens, reset the logged DB password.
2. **Phase 5 = Signage templater + screen OS (docs/09).** ALREADY BUILT (Phase 3, live in prod): `toast_menu_cache` + **`toast-menu-sync`** edge fn (metadata-gated, 297 items / 28 OOS / 61 images mirrored to the `signage` bucket; `publicBlurb` description-safety, unit-tested), `public_menu`/`public_events` views, `show_on_website` flags. Schema ALREADY EXISTS (Phase 0): `signage_slots`, `signage_items`, `screen_takeovers`, `scheduled_events` (+ RLS 0011, realtime 0013). **To build:** (a) `/signage` staff templater — create specials/events from templates (`drink_special`/`event`/`announcement`/`image_only`/`celebration`); `drink_special` with `source_toast_guid` auto-fills name/price/photo from `toast_menu_cache` (live values render GREEN); ★ SCREENS in-stock items auto-materialize into rotation; auto-hide any item whose `source_toast_guid.out_of_stock`; (b) `/signage/s/:slug` public DisplayCanvas board — rotating slots, footer ticker (manual + season top-3 + Toast top-seller, reprint not scroll), takeover broadcasts, celebration animations (all FINITE per perf rules); (c) dashboard sync-freshness panel (docs/12 — last-success timestamps for toast-sync/toast-menu-sync with amber/red staleness). `SignageAdmin` + `SlotDisplay` are Placeholders now.
3. **Also still pending: Phase 3.5 — Public Website (docs/14).** Only the `toast-menu-sync` half was pulled forward; the actual site (`/menu`, `/events`, `/about` pages — currently `modules/website/` placeholders), DNS/MX migration, SEO/Lighthouse are NOT built. `public_menu`/`public_events` + `toast_menu_cache` are ready to power it. Do this or Phase 5 — the user chose Phase 5.
4. Deferred (not blockers): bonus-round badges on Leaderboard; History Load/Duplicate/Delete; "Scoring in Progress" interstitial (needs `rounds.scoring_in_progress`); `Settings.tsx` Placeholder (still a stub — reachable from nav? no, not linked; it's `/settings`). ~~wire Dashboard as host landing page~~ DONE (Phase 4b). **Phase 2:** phone/SMS OTP (config flag; needs Twilio). **Phase 4:** captain transfer in the portal (only remove/invite/PIN built); portal team-card click nav was verified via RPC not click (in-app browser click flakiness — cosmetic).
5. **Owner cleanup owed before production:** ~~(0) CONFIGURE CUSTOM SMTP~~ **DONE 2026-07-12** (Resend, verified delivery, 100/hour — see the ✅ note above). Then: rotate `GEMINI_API_KEY`; **rotate the Toast `TOAST_CLIENT_SECRET`** (pasted in chat 2026-07-12 — now in edge-fn secrets); revoke the migration + Cloudflare tokens; reset the DB password that appeared in a log. **Config NOT in git (re-apply if project rebuilt):** Supabase auth email template (`mailer_otp_length=6` + `{{ .Token }}`); **Resend SMTP config + `rate_limit_email_sent=100`** (`RESEND_API_KEY` in root `.env`); Toast/CRON edge-fn secrets; Vault `cron_secret`; the two pg_cron jobs.

### Testing host tools + players
- **Staff login via `/login`** (email+password). Seeded staff have NO password yet (stephentyler=admin, trashtvronnie=host) — do NOT mint OTP login tokens for their real emails (the sandbox blocks it). To test host tools, recreate a throwaway host: admin-create an auth user with a `*.test` email + `email_confirm:true`, `insert into venue_staff(venue_id, profile_id, role) values('11111111-…', <uid>, 'host')`, mint its OTP via `auth.admin.generateLink({type:'magiclink'})` (returns `email_otp`, sends nothing), `verifyOtp` in the browser console, then DELETE the user + its venue_staff row when done.
- **Player OTP testing:** the client `signInWithOtp` rejects reserved TLDs like `.test` ("Email address is invalid") — but `auth.admin.generateLink` + `verifyOtp` accept them, so drive the auth step that way (mint code server-side, `verifyOtp` in the page console) to avoid burning the low email quota. Real domains (gmail/yahoo/…) work through the full UI.
- **Fixture pattern:** create test games via service-role SQL through the Management API (bypasses RLS) with a recognizable id like `fa11face-0000-4000-8000-0000000000NN`; DELETE when done. Real display test game: `bb00eca3-…-3cad42e` (2026-03-18, 17 teams, wildcards+tiebreakers). Default `/leaderboard` (no `?game=`) resolves to the highest-priority non-completed game (currently the real 2026-07-08 `active` import) — pass `?game=<id>` to pin one.
- **Edge fns** deploy via Management API multipart: `POST /v1/projects/{ref}/functions/deploy?slug=<name>` with `metadata` + `file` parts (see the `analyze-image` deploy in git history). Secrets via `POST /v1/projects/{ref}/secrets`.
- **`GEMINI_API_KEY`** is in root `.env` (gitignored) + set as an edge-fn secret. It was pasted in chat 2026-07-12 — owner may want to rotate it after Phase 1. Ronnie's real deck is at `docs/powerpoint 10903.pptx` (14MB, left UNTRACKED in git — don't commit without deciding).

### Operational notes a fresh session can't see
- **Secrets** live in `.env` + `apps/web/.env` (both gitignored, NOT in git): `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ACCESS_TOKEN` (migration PAT — may be revoked by owner; regenerate at supabase.com/dashboard/account/tokens), `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID=838f6b1ba6d8bf9cc45185679950151b`, `LEGACY_SUPABASE_*` (read-only). Owned project ref: `ysrqvdutayirpoibdlbf`. Venue id: `11111111-1111-1111-1111-111111111111`.
- **Apply migrations:** no Supabase CLI installed — POST each file to `https://api.supabase.com/v1/projects/ysrqvdutayirpoibdlbf/database/query` with `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`. Reusable pattern in prior scratchpad `apply-migrations.mjs` (session-local; re-create if gone).
- **Deploy:** `npm --prefix apps/web run build` then `npx wrangler pages deploy apps/web/dist --project-name=bunker-club-os --branch=main` (needs `CLOUDFLARE_*` in env). Manual until GitHub→Pages auto-deploy is wired.
- **Package manager:** repo standard is pnpm, but this env used `npm install` (no pnpm/corepack offline); no lockfile committed. `apps/web` deps install standalone.
- **Legacy zips** are in repo root; re-unzip to inspect (scratchpad extractions are session-local).
- **Dev server:** `npm --prefix apps/web run dev` (or preview_start "web"). Not running across sessions.
- **Owner cleanup pending:** revoke migration + Cloudflare tokens; reset the DB password that appeared in a log; **rotate `GEMINI_API_KEY`** (pasted in chat 2026-07-12). ~~delete `bunker-club-os-scratch`~~ + ~~delete `qa-host@bunker.test` auth user~~ — both DONE 2026-07-12 via API.

Phase 0 — completed work:
- Repo scaffolded per docs/01 (pnpm monorepo: `apps/web`, `packages/`, `supabase/`, `scripts/`). Git initialized.
- Vite + React 19 + TS(strict) + Tailwind 3 + Router v7 + TanStack Query v5 app shell. Boots with the terminal theme applied; full route map wired with role guards. Displays render through `DisplayCanvas` (fixed-canvas scale-to-fit + `?calibrate` + nightly 04:00 reload).
- Terminal theme moved to `apps/web/src/theme/terminal-theme.css`; third-party franchise wording stripped (principle 5); amber/blue color-state variants scaffolded.
- ALL migrations from docs/02 written (`supabase/migrations/0001–0015`), plus seasons, signage (calibration/recurrence/celebration), and events schema (docs/06/09/13). RLS default-deny with the public/player/staff split; `pin_hash` locked out of anon+authenticated (read AND write); `check_in_team` RPC; `season_leaderboard()` (3 modes). **APPLIED to the owned Supabase project (ref `ysrqvdutayirpoibdlbf`) via the Management API and verified: 18 tables all RLS-on, 36 policies, 5 views, realtime on 12 tables, seed venue present. Anon-key smoke test passed — public reads 200, anon writes + pin_hash reads rejected.**
- **Docs reconciliation (docs/14 public website + updated docs/09 Toast) — migration `0015_public_website.sql` written + APPLIED + verified:** `show_on_website` flag on `signage_items` + `scheduled_events`; public-safe views `public_menu` (blurb = text before `---` per docs/09 safety rule; excludes ★ SCREENS group; exposes in_stock) + `public_events` (tease copy only). Anon SELECT on `toast_menu_cache.description` REVOKED (column-level, mirrors pin_hash) — anon-key test confirms raw `description` → 401, `public_menu`/`public_events`/safe cols → 200. Toast write-access note logged for provisioning: menu STRUCTURE is read-only; only `stock:write` enables bidirectional featured control — request scopes `menus:read, config:read, orders:read, stock:read, stock:write`.
- `scripts/export-legacy.ts` (path B, read-only), `import-legacy.ts` (docs/03 mapping, idempotent, count-verified), `seed-staff.ts`, `backup.ts` (docs/12).
- **Legacy data EXPORTED + IMPORTED. Counts match legacy exactly: teams 265, games 27, rounds 220, scores 1826, questions 1525 (+ game_teams 247, game_display_state 24, venue_settings 49, storage: logos 4 + picture-rounds 31).** Notes: the raw project `cdgdxfichpikapawnnth.supabase.co` is directly readable with the publishable key (gateway bypassed). 22 duplicate team names disambiguated for `unique(venue_id,name)` (originals kept on `game_teams.display_name`); 23 team contacts saved to `legacy-export/unmapped-contacts.json` for Phase 2. Follow-ups for Stephen: 1 customer team name contains a franchise term ("Crawl out through the Fallout"); "Regulars" + that team have true-duplicate regular rows to merge.
- **App boots locally against the live DB; `tsc -b` clean; terminal theme + routing + DisplayCanvas (+`?calibrate`) verified in-browser.**
- **Staff seeded: stephentyler@mac.com (admin), trashtvronnie@gmail.com (host) — provisioned as confirmed auth users (no email sent) so roles are live before Phase 2 login.**
- **DEPLOYED to Cloudflare Pages: https://bunker-club-os.pages.dev (project `bunker-club-os`, auto-builds are NOT connected — deployed via wrangler from local `apps/web/dist`; env baked in at build). Prod smoke-test: deep-link `?calibrate` renders, queried live DB → 265 teams. Custom domain `os.bunkerokc.com` registered on the project; CNAME (os → bunker-club-os.pages.dev) added at Namecheap, propagating.**
- **Backups (docs/12): `backup.ts` runs password-free (service-key JSON of all 18 tables + storage) when `DATABASE_URL` unset, full `pg_dump` when set. Ran locally (4188 rows + 35 objects). Weekly GitHub Action `.github/workflows/backup.yml` proven green in CI; repo secrets SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY set.**
- **RESTORE DRILL PASSED: rebuilt schema from migrations in a scratch project + loaded the backup; all checked table counts matched exactly (teams 265, games 27, rounds 220, scores 1826, questions 1525, …). Auth-dependent tables (profiles/venue_staff/team_members) are excluded from the JSON snapshot by design.**
- **Supabase Pro ACTIVE. Custom domain LIVE: https://os.bunkerokc.com serves the app over valid HTTPS (deep-links work).**

### Phase 0 acceptance (docs/10) — all pass
Custom domain ✅ · Supabase Pro ✅ · backup runs + restore drill ✅ · counts match legacy ✅ · storage present ✅.

Owner follow-ups (non-blocking): ~~delete the `bunker-club-os-scratch` project~~ (DONE 2026-07-12); revoke the migration token (`sbp_…`) and optionally the Cloudflare token; reset the DB password that appeared in a log. Deploy is currently manual `wrangler` from `apps/web/dist` — wire GitHub→Pages auto-deploy (or a deploy workflow) when convenient.

## Commands

`pnpm dev` / `pnpm build` / `pnpm typecheck` · `supabase db push` · `supabase functions deploy <name>`
`pnpm export:legacy` · `pnpm import:legacy` · `pnpm import:contacts` (legacy captains → claimable identities, docs/05) · `pnpm seed:staff` · `pnpm backup`
Tests (tsx, run against the live DB via service role): `pnpm test:businessdate` (toast-sync TZ) · `pnpm test:menutext` (docs/09 description-safety) · `pnpm test:seasons` (season_leaderboard all 3 modes + tiebreaks; seeds + cleans its own fixture).

## Orchestration (from this phase forward)

- The main session runs on Fable and ORCHESTRATES: plans, sequences,
  adjudicates DECISIONs, reviews diffs against specs, manages the PR. It does
  not bulk-write code itself.
- ALL implementation is delegated to the `builder` subagent in scoped tasks
  (one component/migration/function per task, with the relevant docs/ sections
  named in the task prompt).
- Before any phase PR merges, the orchestrator invokes the `reviewer` subagent
  on the branch. The reviewer's report is pasted VERBATIM into the PR body —
  never summarized or edited. Blocking findings are fixed and re-reviewed; any
  waived finding requires the owner's explicit say-so in the session. The
  orchestrator may not merge over an unaddressed finding.

## Rules

- No OptiDev code patterns (gateway client, `window.__ENV__`, `.optidev/` plugins).
- Displays: no infinite animations, no sub-30s polling, realtime-first (one 30–60s fallback poll).
- All season scoring logic lives ONLY in `season_leaderboard()` SQL.
- Venue-scope everything; no hardcoded 'Bunker Club' in logic (theme/config tables instead).
- Original in-world IP only (principle 5): Shelter Authority / BUNKER UNIFIED OS / Civil Defense. NO third-party franchise marks in any rendered output, generated copy, identifiers, or seed data — ever.
- Phases execute in order; acceptance criteria (docs/10) are gates. Each phase = its own branch + PR. When a spec is ambiguous: match legacy → simpler → leave a `// DECISION:` comment and note it in the PR.
- **A phase is complete only when its PR is MERGED to `main` and the deploy workflow (`.github/workflows/deploy.yml`) is green** — merging + a green production deploy is part of finishing the phase, NOT a later owner chore. Record the phase's `phase-*` Cloudflare preview URL in the PR body so reviews can use the running app.
- **Migrations:** during development (pre-cutover) they MAY apply to the live owned project via the Management API. From cutover onward, migrations apply ONLY from merged `main`. Every RLS migration is immediately followed by the anon + host smoke tests (anon writes → 401; host read of the touched tables → 200) before the phase is done. (See docs/12 "CI/CD" + "Migrations: development vs. cutover".)
- **Session handoff (do at the END of every working session, and after each phase):** update the "▶ Next session — START HERE" block + Current state to reflect reality, then `git commit` + `git push`. This file auto-loads next session — it IS the handoff. Keep the "Next session" block to the single most useful next action + any context not in the repo (running services, token state, cleanup owed).
- Durable owner facts/preferences go in the memory system (`memory/`), not here. Repo-derivable facts (schema, file layout, history) stay out of this file.
