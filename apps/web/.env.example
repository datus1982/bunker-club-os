# Frontend env (docs/01). These are the ONLY two the SPA needs, plus the venue id.
# Anon key is public-safe; RLS is the real gate.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_VENUE_ID=11111111-1111-1111-1111-111111111111
