// Toast top-selling drinks admin (docs/08). Ported in Phase 3.
// NB: the public /drinks board is exported from ./displayRoutes instead, so a screen left on
// that legacy route never downloads this admin page. See that file's header.
export { DrinksAdmin } from "./DrinksAdmin";
